# AmseokOS Agent 入口

开始任何分析、修改、验证或文档工作前，必须完整阅读同目录下的 `agent.md`，并遵守其中的架构、阶段计划、进度检查、代码质量、人工提交和交付文案要求

实际项目状态优先于文档中的历史进度记录，每次工作都必须重新检查项目结构和验证结果

通过 SSH 操作测试环境或部署环境时，禁止在目标机暂存、提交、执行会创建提交的合并、变基、推送或创建任何 Git 提交；代码提交只能在本地开发工作区完成，详细边界以 `agent.md` 为准
